If you just cloned this repo set up your enviorment with follow instruction:

install git
install Node.js
cd into app folder
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
npm install
npm run dev